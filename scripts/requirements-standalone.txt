# Keep native release builds on one audited dependency set. The normal Python
# package intentionally keeps compatible ranges; standalone artifacts pin the
# complete HTTP runtime so six parallel builders cannot resolve different bits.
anyio==4.14.2
certifi==2026.7.22
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
